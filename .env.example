VITE_API_URL=

# Optional. Origin that short links point at, without a trailing slash.
# Defaults to VITE_API_URL, since the backend serves the redirect on a bare
# GET /:shortCode. Set this to use a short domain instead,
# e.g. VITE_SHORT_LINK_ORIGIN=https://squish.li
VITE_SHORT_LINK_ORIGIN=
